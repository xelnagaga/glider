# Glider configuration for Render deployment
verbose=true

# Listen on port 10000 (required by Render)
listen=http://:10000

# Forward strategy - direct connection (no proxy)
# Change this to your upstream proxy if needed
# Example: forward=socks5://user:pass@host:1080
forward=direct://

# Strategy
strategy=rr
checkinterval=30

